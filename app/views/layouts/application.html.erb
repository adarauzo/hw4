<!DOCTYPE html>
<html>
  <head>
    <title>Tacostagram Final</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <%= stylesheet_link_tag "application", media: "all" %> </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="/">Tacostagram Final</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto"> 
          <% if logged_in? %>
            <li class="nav-item">
              <p class="navbar-text">Logged in as: <%= current_user.username %></p> 
            </li>
            <li class="nav-item">
              <%= button_to "Logout", session_path(current_user), method: :delete, class: "btn btn-outline-primary" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Login", new_session_path, class: "btn btn-outline-primary"  %>
            </li>
            <li class="nav-item">             
              <%= link_to "Signup", new_user_path, class: "btn btn-outline-primary"  %>
            </li>
          <% end %>
        </ul>
      </div>
    </nav>

    <div class="container">
      <% if flash["notice"] %>
        <div class="alert alert-primary"><%= flash["notice"] %></div>
      <% end %>

      <%= yield %>
    </div>

    <%= javascript_importmap_tags %> </body>
</html>
